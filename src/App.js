// App.js
import React from "react";
import TimerButton from "./TimerButton"; // Import the TimerButton component

const App = () => {
  return (
    <div>
      <TimerButton /> {/* Use the TimerButton component */}
    </div>
  );
};

export default App;
